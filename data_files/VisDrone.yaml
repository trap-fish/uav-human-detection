# Filtered version of the VisDrone dataset with only the human classes remaining

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /media/citi-ai/matthew/uav-human-detection/datasets/filtered/visdrone_humans # dataset root dir
train: train/images # train images (relative to 'path')  6471 images
val: val/images # val images (relative to 'path')  548 images
test: test-dev/images

# Classes
names:
  0: pedestrian
  1: people